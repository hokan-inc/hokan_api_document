{
  "name": "redoc",
  "version": "1.0.0",
  "description": "open api document",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "redoc-cli bundle spec/index.yaml -o public/index.html",
    "develop": "redoc-cli serve spec/index.yaml --watch"
  },
  "dependencies": {
    "redoc-cli": "^0.10.3"
  }
}